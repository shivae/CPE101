#include <stdio.h>
#include "functions.h"
#include "checkit.h"

int main(){
   checkit_double(sum(sum(product(1, 1),(2*(product(1,2)))), product(2,2)), 9);
   checkit_double(sum(sum(product(4, 4),(2*(product(4,3)))), product(3,3)), 49);
   return 0;
}
